-- collision_groups.lua
local groups = {
	PLAYER_GROUP = hash("PLAYER_GROUP"),
	ENEMY_GROUP = hash("ENEMY_GROUP"),
	--obstacle = hash("obstacle_group"),
	-- Add more collision groups as needed
}

return groups
